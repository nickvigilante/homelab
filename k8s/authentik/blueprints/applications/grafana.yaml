version: 1
metadata:
  name: "homelab - application - grafana"
# Grafana OIDC provider + application + group gate. client_secret is injected
# from the authentik-oidc-secrets Secret via !Env (never in the repo). Grafana
# is gated on homelab-users and uses the IMPLICIT consent authorization flow
# (matches the Coder/Outline pattern from #104). client_id `grafana` matches
# grafana.ini auth.generic_oauth.client_id in the kube-prometheus-stack values.
entries:
  - id: grafana-provider
    model: authentik_providers_oauth2.oauth2provider
    state: present
    identifiers:
      name: grafana
    attrs:
      client_type: confidential
      client_id: grafana
      client_secret: !Env [AUTHENTIK_OIDC_GRAFANA_SECRET, ""]
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
      redirect_uris:
        - matching_mode: strict
          url: https://grafana.vigihome.net/login/generic_oauth
  - id: grafana-app
    model: authentik_core.application
    state: present
    identifiers:
      slug: grafana
    attrs:
      name: Grafana
      provider: !KeyOf grafana-provider
      meta_launch_url: https://grafana.vigihome.net
      policy_engine_mode: any
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !KeyOf grafana-app
      group: !Find [authentik_core.group, [name, homelab-users]]
      order: 0
    attrs:
      enabled: true
      negate: false
