# Only the ExternalSecret is Flux-owned for now -- the Outline app,
# postgres chart, redis Deployment, PV/PVC, NetworkPolicies, Ingress,
# etc. stay manually-managed (full takeover is future work).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secret.yaml
